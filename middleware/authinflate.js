export default function (ctx) {

  // console.log(ctx.$auth)

  // // check if JWT user is logged in
  // const jwtLoggedIn = true;

  // // check if redux user is logged in
  // const reduxLoggedIn = true;

  // if (ctx.$auth.$state.loggedIn && ctx.$store.state.user === null) {
  
  //   // get user info from jwt token
  //   const user = null;

  //   // dispatch
  //   store.dispatch('setUser', user);  
  // }

  return
}